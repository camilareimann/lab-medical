import { GenderPicturePipe } from './gender-picture.pipe';

describe('GenderPicturePipe', () => {
  it('create an instance', () => {
    const pipe = new GenderPicturePipe();
    expect(pipe).toBeTruthy();
  });
});
